<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ScrollPane?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.layout.VBox?>

<BorderPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1" fx:controller="org.tcp.grupo01.controller.TournamentDetailsController">

    <left>
        <VBox styleClass="sidebar" spacing="20" prefWidth="250">
            <padding><Insets top="30" right="20" bottom="20" left="20"/></padding>
            <VBox spacing="5">
                <Label fx:id="lblTournamentName" styleClass="sidebar-title" text="Nome do Torneio" wrapText="true"/>
                <Label fx:id="lblStatus" styleClass="sidebar-subtitle" text="Status"/>
            </VBox>
            <VBox spacing="10">
                <Label text="INFORMAÇÕES GERAIS" styleClass="sidebar-section-title"/>
                <Label fx:id="lblParticipantsCount" text="0 participantes" styleClass="sidebar-text"/>
            </VBox>
        </VBox>
    </left>

    <center>
        <VBox spacing="30" styleClass="main-content">
            <padding><Insets top="30" right="40" bottom="30" left="40"/></padding>

            <HBox alignment="CENTER_LEFT">
                <Button text="← Voltar aos Campeonatos" onAction="#handleBack" styleClass="btn-back-link"/>

                <Region HBox.hgrow="ALWAYS"/>

                <HBox styleClass="segmented-control">
                    <Button fx:id="btnTabRounds" text="Rodadas" onAction="#showRounds" styleClass="segment-button"/>
                    <Button fx:id="btnTabStandings" text="Classificação" onAction="#showStandings" styleClass="segment-button"/>
                </HBox>
            </HBox>

            <HBox alignment="CENTER_LEFT" spacing="20">
                <Label fx:id="lblSectionTitle" text="Rodada 1" styleClass="page-title"/>

                <Region HBox.hgrow="ALWAYS"/>

                <HBox fx:id="paginationControls" spacing="10" alignment="CENTER_RIGHT">
                    <Button fx:id="btnPrevRound" text="‹ Anterior" onAction="#handlePrevRound" styleClass="btn-pagination"/>
                    <Button fx:id="btnNextRound" text="Próxima ›" onAction="#handleNextRound" styleClass="btn-pagination"/>
                </HBox>
            </HBox>

            <StackPane VBox.vgrow="ALWAYS">

                <ScrollPane fx:id="roundsContainer" fitToWidth="true" styleClass="transparent-scroll">
                    <VBox fx:id="matchesList" spacing="15">
                    </VBox>
                </ScrollPane>

                <VBox fx:id="standingsContainer" visible="false">
                    <HBox styleClass="standings-placeholder" alignment="CENTER_LEFT" spacing="15">
                        <Label text="🏆" style="-fx-font-size: 24px;"/>
                        <VBox>
                            <Label text="Tabela de Classificação" style="-fx-font-weight: bold; -fx-font-size: 16px;"/>
                            <Label text="Disponível em breve" style="-fx-text-fill: #666;"/>
                        </VBox>
                    </HBox>
                </VBox>

            </StackPane>
        </VBox>
    </center>
</BorderPane>